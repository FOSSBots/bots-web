dist: xenial
os: linux
language: node_js
node_js:
  - node
install:
  - npm ci
before_script:
  - node --version
  - npm --version
script:
  - npm test
notifications:
  irc:
    channels:
      - "chat.freenode.net#ZppixBot"
    on_success: change
    on_failure: always
    template:
      - "%{repository}/%{branch}/%{commit} - %{author} %{message} %{build_url}"
