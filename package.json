{
  "private": true,
  "scripts": {
    "test": "npm run stylelint && npm run htmllint",
    "stylelint": "stylelint 'public_html/**/*.css'",
    "htmllint": "htmllint 'public_html/**/*.html'"
  },
  "devDependencies": {
    "htmllint": "0.7.3",
    "htmllint-cli": "0.0.7",
    "stylelint": "^13.8.0",
    "stylelint-config-standard": "^20.0.0"
  }
}
